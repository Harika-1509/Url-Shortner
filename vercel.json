{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd client-app && npm ci",
  "buildCommand": "cd client-app && npm run build",
  "outputDirectory": "client-app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
